name: Edge CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install project
        run: python -m pip install -e ".[dev]"

      - name: Validate and lint files
        run: pre-commit run --all-files --hook-stage pre-commit

      - name: Validate pull-request commit messages
        if: github.event_name == 'pull_request'
        run: >-
          python scripts/check_commit_message.py
          --range "${{ github.event.pull_request.base.sha }}..${{ github.event.pull_request.head.sha }}"

      - name: Validate pushed commit messages
        if: github.event_name == 'push'
        run: >-
          python scripts/check_commit_message.py
          --range "${{ github.event.before }}..${{ github.sha }}"

      - name: Validate branch name
        env:
          BRANCH_NAME: ${{ github.head_ref || github.ref_name }}
        run: python scripts/check_branch_name.py "$BRANCH_NAME"

      - name: Type-check and test
        run: python scripts/run_pre_push.py
